<%= form_with(model: movie_genre) do |form| %>
  <% if movie_genre.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie_genre.errors.count, "error") %> prohibited this movie_genre from being saved:</h2>

      <ul>
        <% movie_genre.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-xs-4">
      <%= form.label :name, style: "display: block", :class => 'mt-4' %>
      <%= form.text_field :name, class: "bg-dark text-white rounded form-control mb-4" %>
    </div>

    <div class="float-right">
      <%= link_to "Cancelar", movie_genres_path, class: "btn light-gray-3 space-button"%>
      <%= form.submit 'Salvar', :class => 'btn primary'%>
    </div>
<% end %>