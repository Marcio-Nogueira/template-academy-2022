<p style="color: green"><%= notice %></p>
<div id="artists" class="d-flex mb-5 justify-content-between">
<h1>Movie genres</h1>
<a href="<%= new_movie_genre_path %>"><button class="btn" style="background-color: #FF375F; color: white">+ Novo</button></a>

</div>
<table class="table table-striped table-dark">
  <thead>
    <tr>
      <th class="col-md-3" scope="col">Nome</th>
      <th class="col-md-3" scope="col">Criado em</th>
      <th class="col-md-3 text-right" scope="col" style='text-align: right'>Ações</th>
    </tr>
  </thead>
    <tbody>
      <% @movie_genres.each do |movie_genre| %>
        <%= render movie_genre %>
      <% end %>
    </tbody>
</table>